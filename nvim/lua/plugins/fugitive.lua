-- TODO: there are definitely better git plugins
-- later... easy does if bud. Fugitive is still good may be worth
-- checking out what else is out there but `Git blame` is table stakes
return {
  {
    "tpope/vim-fugitive"
  }
}
